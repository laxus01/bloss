export interface Images {
  src: string;
  size: number;
}

export interface Characters{
  id: string;
  image: string;
  name: string;
  species: string;
  status: string;
  like: boolean;
}

export interface Bottons{
  text: string;
}
